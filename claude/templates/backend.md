# Backend Rules
> 루트 CLAUDE.md의 모든 규칙을 상속하며, 충돌 시 이 파일이 우선한다.

## Scope
API, 서버 로직, DB, auth, cache, queue, background job 변경에 적용한다.

## Before changing backend code
- 입력, 출력, 에러 형식, side effect를 먼저 적는다.
- 이 변경이 계약 변경(외부에 보이는 것)인지 내부 구현 변경인지 먼저 구분한다.
- 계약이 바뀌면 호출자(프론트엔드, 다른 서비스, 스크립트) 영향까지 같이 본다.

## Change rules
- 요청받지 않은 endpoint, 옵션, 설정값을 추가하지 않는다.
- 한 번만 쓰는 로직을 위해 과한 추상화를 만들지 않는다.
- 민감정보(토큰, 비밀번호, 개인정보)가 로그에 남지 않게 한다.
- 실제로 발생 가능한 실패만 처리한다.
- 성능 최적화는 측정 근거가 있을 때만 한다.

## Verification
- 가능하면 재현 가능한 입력/출력으로 확인한다.
- 관련 테스트가 있으면 먼저 그 경로로 검증한다.
- DB/auth/cache 영향이 있으면 정상 경로 + 실패 경로 둘 다 확인한다.
- 응답 형식이 바뀌었으면 호출자 측 테스트도 같이 본다.
